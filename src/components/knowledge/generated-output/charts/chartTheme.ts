export const chartToneClasses = {
    primary: "bg-primary",
    success: "bg-success",
    warning: "bg-warning",
    danger: "bg-danger",
} as const;

export const donutPalette = ["var(--primary)", "var(--secondary)", "var(--warning)", "var(--muted)"];
