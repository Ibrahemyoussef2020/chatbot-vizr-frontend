const BusinessPlans = () => (
    <div className="mx-auto w-full max-w-[1400px] p-2">
        <header className="border-b border-border pb-5">
            <span className="text-xs font-extrabold uppercase tracking-[.14em] text-primary">Business Owner</span>
            <h1 className="mb-1 mt-1 text-3xl font-extrabold text-foreground">Plans</h1>
        </header>
    </div>
);

export default BusinessPlans;
